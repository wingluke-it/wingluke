import React from 'react';
import { useRootTheme } from './hooks';
import { ThemeProvider } from './themeProvider';
export function ThemeColorProvider(props) {
    const { children, scheme, variant } = props;
    const root = useRootTheme();
    return (React.createElement(ThemeProvider, { scheme: scheme || root.scheme, theme: root.theme, variant: variant }, children));
}
//# sourceMappingURL=themeColorProvider.js.map