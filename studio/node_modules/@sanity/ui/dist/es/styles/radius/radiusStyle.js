import { getResponsiveProp, rem, responsive } from '../helpers';
export function responsiveRadiusStyle(props) {
    const { theme } = props;
    return responsive(theme.sanity.media, getResponsiveProp(props.radius).map((radiusIndex) => ({
        borderRadius: rem(theme.sanity.radius[radiusIndex]),
    })));
}
//# sourceMappingURL=radiusStyle.js.map