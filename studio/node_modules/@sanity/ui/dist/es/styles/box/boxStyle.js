import { getResponsiveProp, responsive } from '../helpers';
const BOX_SIZING = {
    content: 'content-box',
    border: 'border-box',
};
const HEIGHTS = {
    stretch: 'stretch',
    fill: '100%',
};
export function boxStyle({ height, overflow, sizing }) {
    return [
        {
            boxSizing: sizing && BOX_SIZING[sizing],
            height: height && HEIGHTS[height],
            overflow,
        },
        responsiveBoxDisplayStyle,
    ];
}
function responsiveBoxDisplayStyle({ display, theme }) {
    return responsive(theme.sanity.media, getResponsiveProp(display).map((val) => ({ '&:not([hidden])': { display: val } })));
}
//# sourceMappingURL=boxStyle.js.map