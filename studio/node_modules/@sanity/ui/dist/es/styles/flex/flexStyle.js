export function flexStyle(props) {
    return [
        {
            alignItems: props.align,
            justifyContent: props.justify,
            '&:not([hidden])': {
                display: 'flex',
            },
        },
        responsiveFlexColumnStyle,
    ];
}
export function responsiveFlexColumnStyle(props) {
    if (props.direction && props.direction.startsWith('column')) {
        return {
            flexDirection: props.direction,
            flexWrap: props.wrap,
            '&>*': {
                minHeight: 0,
            },
        };
    }
    return {
        flexDirection: props.direction,
        flexWrap: props.wrap,
        '&>*': {
            minWidth: 0,
        },
    };
}
//# sourceMappingURL=flexStyle.js.map