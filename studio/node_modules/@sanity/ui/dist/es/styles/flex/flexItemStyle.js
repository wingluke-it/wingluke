import { getResponsiveProp, responsive } from '../helpers';
export function flexItemStyle({ flex, theme }) {
    return responsive(theme.sanity.media, getResponsiveProp(flex).map((val) => ({ flex: val })));
}
//# sourceMappingURL=flexItemStyle.js.map