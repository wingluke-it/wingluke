import { UndoRedoStack } from '../typeDefs';
declare type Options = {
    stack: UndoRedoStack;
};
export default function UndoRedoPlugin(options: Options): {
    onKeyDown(event: React.KeyboardEvent<any>, editor: any, next: (arg0: void) => void): any;
};
export {};
