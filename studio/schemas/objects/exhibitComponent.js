/**
 * see: https://www.informalscience.org/sites/default/files/VSA-a0a1v7-a_5730.pdf
 * contains: artifacts, text (exhibit cards), communication media
 * artifacts: either a document type in sanity, or a string field that's the same as the artifact's ID in the SQL database. then on Gatsby's end it pulls in the artifact's info using the string ID during build time?
 */
